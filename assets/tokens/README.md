# Token assets

Сюда переносятся токены для Foundry Actor.

## Подпапки

- `npcs/` — токены НИПов;
- `creatures/` — токены существ и монстров.

Пути должны совпадать с `assets/manifests/old-world-assets.json`.

До фактического переноса файлов записи в манифесте держатся в статусе `pending-import`.
